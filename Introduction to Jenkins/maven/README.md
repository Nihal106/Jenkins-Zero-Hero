# Maven + Jenkins CI/CD Demo

This project demonstrates how to build a Java application using Maven and integrate it with a Jenkins CI/CD pipeline.

## 🔹 About Maven
Apache Maven is a build automation and dependency management tool used for Java projects. It provides:

- Dependency management
- Build automation (compile, test, package)
- Standard project structure
- Plugin support
- Easy CI/CD integration

## 🔹 Build Commands

Compile:
    mvn clean compile

Run tests:
    mvn test

Package:
    mvn clean package
